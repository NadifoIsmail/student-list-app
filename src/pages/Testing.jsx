/* import React from "react";

export default function Testing({ mastude }) {
  return mastude.map((stude) => (
    <div className="row row-cols-1 row-cols-md-4 g-4">
      <div className="col" key={stude.id}>
        <div className="card">
          <img src={stude.image} class="card-img-top" alt={`${stude.name}'s profile`} />
          <div className="card-body">
            <h5 className="card-title">{stude.name}</h5>
            <p className="card-text">
              Age:{stude.age}<br/>
              Course:{stude.course}
            </p>
          </div>
        </div>
      </div>
    </div>
  ));
} */
